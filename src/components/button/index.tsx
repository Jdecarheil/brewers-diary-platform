// import { Button } from '../ui/button';

// interface ButtonProp {
//   onClick: () => void;
//   variant: string;
//   size: string;
//   title: string;
// }

// export const ButtonDefault = (props: ButtonProp) => {
//   const { onClick, variant, size, title } = props;
//   return (
//     // <Button size={size} onClick={onClick} variant={'default'}>
//     //   {title}
//     // </Button>
//   );
// };
